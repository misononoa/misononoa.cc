---
import { Icon } from "astro-icon/components";
import Nav, { type NavLink } from "./Nav.vue";

export interface Props {
	title: string;
	iconName: string;
}
const { title, iconName } = Astro.props;
const linkprops: NavLink[] = [
	{
		href: "/",
		name: "misononoa.cc",
	},
	{
		href: "/about",
		name: "about_me",
	},
	{
		href: "/links",
		name: "links",
	},
	{
		href: "/blog",
		name: "blog",
	},
	{
		href: "https://msky.misononoa.cc/",
		name: "misskey",
	},
];
---

<header>
	<h1>
		<Icon name={`fluent-emoji-flat:${iconName}`} height={25} class="icon" />
		{title}
	</h1>
	<!-- <Navigation client:idle current={title} links={linkprops} /> -->
	<Nav client:idle current={title} links={linkprops} />
</header>

<style>
	header {
		height: max-content;
		width: 100%;
		margin: 5ch 0 3ch 0;
	}

	h1 {
		margin: 1rem 0;
		font-size: 170%;
	}

	.icon {
		scale: 150%;
	}

	@media screen and (max-width: 450px) {
		h1 {
			font-size: 150%;
		}
	}
</style>
