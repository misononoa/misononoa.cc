---
export interface Props {
	fqdn: string;
	username: string;
	text: string;
}

const { fqdn, username, text } = Astro.props;
---

<p><a href={`https://${fqdn}/@${username}`}>{text}</a></p>

<style>
	p a {
		font-size: large;
		padding: 0;
		margin: 0;
		text-decoration: none;
	}
</style>
